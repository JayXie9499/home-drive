{
  "name": "home-drive",
  "description": "",
  "version": "0.1.0",
  "author": "Jay",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "lint:backend": "pnpm lint --prefix backend",
    "format:backend": "pnpm format --prefix backend",
    "build:backend": "pnpm build --prefix backend",
    "start:backend": "pnpm start --prefix backend",
    "dev:backend": "pnpm dev --prefix backend",
    "lint:frontend": "pnpm lint --prefix frontend",
    "format:frontend": "pnpm format --prefix frontend",
    "build:frontend": "pnpm build --prefix frontend",
    "start:frontend": "pnpm preview --prefix frontend",
    "dev:frontend": "pnpm dev --prefix frontend",
    "dev": "concurrently \"pnpm dev:backend\" \"pnpm dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
